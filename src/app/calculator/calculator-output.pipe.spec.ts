import { CalculatorOutputPipe } from './calculator-output.pipe';

describe('CalculatorOutputPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculatorOutputPipe();
    expect(pipe).toBeTruthy();
  });
});
